# 🚀 Network Device Monitor & Diagnostic Tool 利用手順書

**対象読者**: 本システムを日常的に使用するネットワーク運用担当者・現場管理者の方

---

## 📋 目次

1. [システムの起動と終了](#1-システムの起動と終了)
2. [ダッシュボードの見方とリアルタイム監視](#2-ダッシュボードの見方とリアルタイム監視)
3. [⏱ 到達性ヒートマップ（タイムライン）の活用](#3-到達性ヒートマップタイムラインの活用)
4. [機器の登録・編集・グループ管理（デバイス管理）](#4-機器の登録編集グループ管理デバイス管理)
5. [⚙️ 設定（監視・通知・バックアップ・保守）](#5-️-設定監視通知バックアップ保守)
6. [ネットワークトポロジー図（System Topology）](#6-ネットワークトポロジー図system-topology)
7. [詳細診断モーダル（ダブルクリックで起動）](#7-詳細診断モーダルダブルクリックで起動)
8. [帯域計測（Iperf3 計測）](#8-帯域計測iperf3-計測)
9. [ログ・CSVサマリーレポートの確認](#9-ログcsvサマリーレポートの確認)
10. [よくある質問・トラブルシューティング](#10-よくある質問トラブルシューティング)

---

## 1. システムの起動と終了

### 起動手順
1. 本フォルダ内にある **`Start-Monitor.bat`** をダブルクリックします。
2. 黒いコマンド画面（PowerShell）が起動します。これは **監視処理を行うバックグラウンドサーバー** です。**監視中は閉じないでください。**
3. 数秒後に自動でブラウザが起動し、監視ダッシュボード（`http://localhost:8081`）が表示されます。

> **💡 ヒント**: 以前に登録した機器情報や設定（表示位置・閾値・通知設定など）はすべて自動的に読み込まれます。

### 終了手順
1. ブラウザのタブを閉じるか、画面操作を終了します。
2. バックグラウンドで起動している **黒いPowerShell画面で [Enter] キーを押すか、ウィンドウを閉じます**。
3. 終了時に直近の監視セッションサマリー（パケットロス率・ジッター・最大瞬断時間など）がCSVファイルに自動保存されます。

---

## 2. ダッシュボードの見方とリアルタイム監視

ダッシュボードでは、登録された全機器の通信健全性をリアルタイムに一目で監視できます。

### 画面上部・サイドバーのサマリーバッジ
サイドバー上部には、システム全体の稼働ステータスがリアルタイム集計されます。
- 🟢 **正常 ○台**: 正常に応答（Ping成功）している機器数
- 🔴 **障害 ○台**: タイムアウト等で応答がない機器数
- ⏸ **停止 ○台**: 監視を一時停止している機器数

### 各機器カードの表示項目

```
+-----------------------------------------------------------------------------------------------+
| [●] コアスイッチ (0.1s)      ↑ TX: 12.4 Mbps    ロス: 0.0%       1.2 ms (良好)               |
|     192.168.1.1 / MAC: ...   ↓ RX: 45.8 Mbps    揺らぎ: 0.3ms    最大 4.5ms / 最小 0.8ms     |
|                                                                  瞬断最大: なし / 600ms~: 0回 |
+-----------------------------------------------------------------------------------------------+
```

| 表示項目 | 説明 | 健全性の目安 |
| :--- | :--- | :--- |
| **ステータス点 [●]** | 🟢 緑（正常） / 🔴 赤（障害・オフライン） / ⏸ 灰（一時停止） | 障害時は自動でアラート発報 |
| **機器名 / IP / レート** | 機器名とIPアドレス。デュアルレート時は `0.1s`（超高頻度）または `5.0s` バッジを表示 | - |
| **TX / RX 通信量** | SNMPから取得したリアルタイムの送信（↑TX）/ 受信（↓RX）レート | 帯域逼迫の早期発見 |
| **パケットロス率 (%)** | 直近サンプリングにおけるパケット損失の割合 | **0.0% が理想**。5%を超えると赤字警告 |
| **揺らぎ (ジッター ms)** | RFC 3550 規格に準拠した遅延のばらつき（パケット揺らぎ） | **< 10ms が良好**。音声/映像通信の品質指標 |
| **遅延時間 (ms)** | 現在のPing応答時間。設定した遅延閾値（例: 100ms）を超えると黄色・赤字に変化 | ネットワーク混雑の検知 |
| **最大 / 最小 (ms)** | 監視開始からのセッション内最小・最大遅延 | スパイク発生の有無を確認 |
| **瞬断最大 / 瞬断中** | ・正常時: 過去に発生した**最大連続オフライン時間**（例: `瞬断最大: 1.2秒`）<br>・障害中: 現在継続している停止時間（赤字で `瞬断中: 15.0秒`） | 瞬断の深刻度を即座に判定 |
| **一時停止 / 再開ボタン** | [⏸] / [▶] ボタンで機器ごとの監視を即座に一時停止・再開可能 | メンテナンス時のアラート抑止 |

### 📈 折れ線グラフの連携機能（ホバーフォーカスハイライト）
- 機器カードの上にマウスカーソルを乗せると、右側の統合グラフで**該当機器の折れ線以外がフェードアウト（半透明化）**し、注目している機器の波形が一目で際立ちます。
- グラフ下部の凡例（レジェンド）をクリックすることで、特定の機器のみを表示/非表示に切り替えることも可能です。

---

## 3. ⏱ 到達性ヒートマップ（タイムライン）の活用

ダッシュボード上部に配置された **「⏱ 稼働タイムライン (ヒートマップ)」** では、各機器の直近の通信品質がカラーブロックで時系列表示されます。

- 🟢 **緑色ブロック**: 正常通信（低遅延）
- 🟡 **黄色ブロック**: 高遅延検知（閾値超過）
- 🔴 **赤色ブロック**: パケット不達・障害発生（オフライン）
- ⚪ **灰色ブロック**: 監視一時停止中 / データなし

過去にどのような周期で遅延や瞬断が発生していたかをスクロールなしで直感的に俯瞰できます。

---

## 4. 機器の登録・編集・グループ管理（デバイス管理）

左サイドバーの **「デバイス管理」** タブから新しい機器の追加や設定変更を行います。

### 機器の追加手順
1. **IP アドレス**: 監視対象のIP（例: `192.168.10.1`）を入力（必須）
2. **デバイス名**: 表示名（例: `フロアスイッチ-2F`）を入力（必須）
3. **グループ**: 所属するグループを選択、または新規作成（例: `2Fオフィス`, `サーバー室`）
4. **アイコン**: 機器種別に応じたアイコン（Switch, Router, AP, Camera, PC 等）を選択
5. **SNMP設定 (任意)**:
   - **v2c**: コミュニティ名（通常 `public`）を入力
   - **v3**: ユーザー名、認証プロトコル/パスワード、暗号化プロトコル/パスワードを入力
6. **「機器を追加」** ボタンをクリックすると即座に監視が開始されます。

---

## 5. ⚙️ 設定（監視・通知・バックアップ・保守）

設定画面では、監視パラメータ・通知・バックアップ・保守の管理を行えます。

### ① 監視・閾値タブ
- **基本ポーリング間隔 (ms)**: 通常監視の実行間隔（デフォルト: `1000ms`）。
- **0.1s 超高頻度デュアルレート監視**:
  - `100ms` を選択すると、**最重要ターゲット最大2台を0.1秒間隔、その他の機器を5.0秒間隔** で並行監視するデュアルレートモードが発動します。
  - 対象機器①・②をドロップダウンから選択します。
- **Ping 送信データサイズ (bytes)**: ICMP Echo ペイロードサイズ（デフォルト: `1 byte`、最大: `65,500 bytes`）。
- **遅延アラート閾値 (ms)**: 警告を発するPing遅延時間（デフォルト: `100ms`）。
- **瞬断判定閾値① / ② (ms)**: 瞬断回数をカウントする判定閾値（デフォルト: `600ms` / `5000ms`）。

### ② 通知・音声タブ
- **🔊 Web Audio 音声アラート**:
  - 障害検知時（オフライン化・高遅延時）にブラウザ上で警告音（ビープ音/チャイム）を自動生成して鳴らします。
  - 音量スライダーで音量を調節し、「🔊 テスト再生」で事前に確認できます。
- **🔔 Webhook 外部通知**:
  - Slack, Microsoft Teams, Discord 等の Incoming Webhook URL を登録することで、障害発生・復旧時に自動で通知メッセージを送信します。
  - 「🔔 テスト送信」ボタンで、URLの疎通確認が可能です。

### ③ バックアップ・復元タブ
- **📥 設定バックアップのダウンロード**:
  - 登録されたすべての機器情報（IP、名前、グループ、SNMP設定、トポロジー座標）とシステムパラメータを1つの `.json` ファイルとしてダウンロードします。
- **📤 バックアップから復元**:
  - バックアップ `.json` ファイルを選択して「適用・復元」を押すだけで、別PCへの移行や過去の構成への復元が1クリックで完了します。

### ④ ログ保守タブ
- **ログ自動クリーンアップ保持日数**:
  - 指定した日数（例: `30日`）を超過した古いセッションログフォルダーを自動的に安全削除し、ストレージ容量の逼迫を防ぎます。

---

## 6. ネットワークトポロジー図（System Topology）

左サイドバーの **「トポロジーマップ」** タブで、ネットワーク全体の物理・論理接続構成をグラフィカルに管理できます。

- **動的リンクアラート**:
  - 正常時: クリーンな **青色（有線）/ シアン破線（無線）**
  - 高遅延時: **琥珀色（黄色）** に変化
  - 障害・断線時: **赤色点線** で強調表示
- **ノード配置の自由移動 & 接続線追加**:
  - ドラッグして機器を自由に配置。「レイアウト保存」で次回起動時も位置を完全維持します。
- **自動検出**: SNMPが有効な機器同士は、CDP/LLDP情報をもとに自動で接続線が結ばれます。

---

## 7. 詳細診断モーダル（ダブルクリックで起動）

ダッシュボード上の機器カードを **ダブルクリック** すると、エンジニア向けの詳細診断画面が開きます。

1. **📊 ポート・トラフィック詳細**:
   - 各ネットワークポートの通信速度（Mbps）、エラーパケット数、破棄パケット数をリアルタイム監視。
2. **📍 経路診断（MTR）**:
   - 対象機器までのルーティングホップを自動追跡し、各ルーターでの遅延とパケットロス率を診断。
3. **📈 24時間 履歴トレンド**:
   - 過去24時間の遅延・通信量の推移をグラフで遡って確認。

---

## 8. 帯域計測（Iperf3 計測）

回線の実効通信速度（スループット）を測定するツールです。

1. サイドバーの **「Iperf3 計測」** タブを開きます。
2. 対象PCを選択するか、「カスタム入力」でIPアドレスを指定します。
3. 計測秒数（例: 10秒）を指定して **「計測開始」** をクリックします。
4. 1秒ごとのスループットグラフとコンソール出力がリアルタイムに表示され、完了時に `Reports/` フォルダへログが自動保存されます。

---

## 9. ログ・CSVサマリーレポートの確認

監視中のデータはすべて `Reports/<起動日時>/` フォルダ内に自動整理されて保存されます。

### 自動出力されるファイル
1. **`ping_<IPアドレス>.csv`**:
   - 監視中の全Ping結果（タイムスタンプ、遅延ms、送受信帯域）が記録されます。
   - **セッション終了時に日本語サマリーブロックが末尾に自動追記** されます：
     ```csv
     --- 計測サマリー ---
     セッション（計測日時）,2026-08-17 19:00:00
     IPアドレス,192.168.10.1
     総Ping送信数（試行回数）,3600
     成功数（応答あり）,3598
     失敗数（応答なし・タイムアウト）,2
     到達率 / 接続性 (%),99.94
     パケット損失率 (%),0.06
     平均ジッター (ms),0.42
     最小遅延 (ms),1.1
     最大遅延 (ms),12.4
     平均遅延 (ms),1.8
     最大瞬断時間（秒）,1.2
     600ms以上の瞬断回数,1
     5s以上の瞬断回数,0
     備考,オフラインからオンラインへ復帰した時点でカウント
     ```
2. **`iperf_<対象>_<日時>.log`**: Iperf3帯域計測の実行ログ。

---

## 10. よくある質問・トラブルシューティング

| 現象 | 原因と対処法 |
| :--- | :--- |
| **画面が開かない / 接続エラー** | バックグラウンドの PowerShell 画面が閉じられていないか確認してください。再度 `Start-Monitor.bat` を実行してください。 |
| **Pingがすべてタイムアウト（赤）になる** | 対象機器側で Windows Defender ファイアウォール等の「ICMP Echo（Ping）の許可」が有効になっているか確認してください。 |
| **SNMPの情報（TX/RX）が取得できない** | 機器側のSNMPコミュニティ名（`public`等）または v3 認証情報が正しいか確認してください。 |
| **音声アラートが鳴らない** | ブラウザの自動再生ポリシーにより、画面の初回クリック操作後に音声が有効化されます。また、設定モーダルで「音声アラート」がONになっているか確認してください。 |
| **ディスク容量が心配** | 「ログ保守」機能で保持日数（例: 14日や30日）を設定すれば、古いログは自動的に削除されます。 |
