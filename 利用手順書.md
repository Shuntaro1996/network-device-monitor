# 🚀 Network Device Monitor & Diagnostic Tool 利用手順書

**対象読者**: このシステムを日常的に使用する運用担当者・ネットワーク管理者の方

---

## 📋 目次

1. [起動方法](#1-起動方法)
2. [機器の登録と管理（Add Device / Manage）](#2-機器の登録と管理)
3. [ダッシュボードの見方（Dashboard）](#3-ダッシュボードの見方)
4. [ネットワークトポロジー図（System Topology）](#4-ネットワークトポロジー図)
5. [詳細診断モーダル（デバイスをダブルクリック）](#5-詳細診断モーダル)
6. [帯域計測（Iperf3 計測）](#6-帯域計測iperf3-計測)
7. [監視設定の変更](#7-監視設定の変更)
8. [ログ・レポートの確認](#8-ログレポートの確認)
9. [終了方法](#9-終了方法)

---

## 1. 起動方法

1. 本フォルダにある **`Start-Monitor.bat`** をダブルクリックします。
2. 黒いコマンド画面（PowerShell）が起動します。これは **バックグラウンドで動き続けるサーバー**です。**絶対に閉じないでください。**
3. 数秒後、自動的にWebブラウザが立ち上がり、監視画面（`http://localhost:8081`）が表示されます。

> **💡 ヒント**: 次回以降も同じ手順で起動します。以前に登録した機器情報は自動的に読み込まれます。

---

## 2. 機器の登録と管理

### 機器を追加する

1. 左サイドバーの **「Add Device / Manage」** タブをクリックします。
2. 以下の項目を入力します。

   | 項目 | 説明 | 必須 |
   |------|------|------|
   | **IP アドレス** | 監視対象機器のIPアドレス（例: `192.168.1.1`） | ✅ |
   | **デバイス名** | 一覧に表示する名前（例: `コアスイッチ`） | ✅ |
   | **グループ** | 機器を分類するグループ名（例: `1F`, `サーバー室`） | 任意 |
   | **アイコン画像** | 機器の種類に応じたアイコン（AP, Camera, Switch など） | 任意 |

3. SNMP詳細情報（インターフェース使用量など）を取得したい場合は、**「SNMP設定」** を展開して入力します。
   - **v2c**: コミュニティ名（通常 `public`）
   - **v3**: ユーザー名・認証プロトコル・パスワードなど

4. **「Add Device」** ボタンを押すと、一覧に追加されて監視が開始されます。

### グループの確認と利用

- 機器登録時に設定したグループ名は、**「Add Device / Manage」画面のドロップダウン**で確認・変更できます。
- グループを使うと、関連する機器をまとめてトポロジー図に表示したり、フィルタリングに活用できます。

### 機器の削除・設定変更

- 登録済みの機器は一覧に表示されます。各機器の行から削除・編集が可能です。

---

## 3. ダッシュボードの見方

ダッシュボードには、監視中の全機器がリアルタイムで表示されます。

### 各列の意味

| 表示項目 | 内容 |
|----------|------|
| **● (ステータス点)** | 🟢 緑 = オンライン、🔴 赤 = オフライン（Pingタイムアウト） |
| **デバイス名** | 登録した機器名 |
| **IP** | IPアドレス |
| **Latency (ms)** | Pingの応答時間。設定した閾値を超えると 🟡 黄色 → 🔴 赤に変わります |
| **Min / Max (ms)** | 現在の監視セッション中における **最小・最大遅延** |
| **BW (Mbps)** | SNMPで取得したリアルタイムの通信量 |
| **Last Seen** | 最後にオンラインを確認した日時 |

> **💡 Min/Max について**: 監視セッション開始（`Start-Monitor.bat`起動）からの累計値です。サーバーを再起動するとリセットされます。

---

## 4. ネットワークトポロジー図

左サイドバーの **「System Topology」** タブで、機器の接続関係を視覚的に確認できます。

### 操作方法

| モード | 操作 |
|--------|------|
| **移動モード**（デフォルト） | 機器のノードをドラッグして位置を自由に配置できます |
| **接続モード** | ノードをドラッグすると接続線（リンク）を引くことができます |

- **位置・接続の保存**: 「保存」ボタンを押すと現在のレイアウトが保存されます。次回起動時も同じ配置で表示されます。
- **自動接続**: CDP/LLDPによる自動トポロジー検出機能があります（SNMP有効機器のみ）。

---

## 5. 詳細診断モーダル

ダッシュボードの機器行を **ダブルクリック** すると、詳細情報ウィンドウが開きます。

### 📊 インターフェース詳細

- ポートごとの通信量（受信/送信 Mbps）をリアルタイム表示
- **エラーパケット数**・**破棄パケット数** を監視できます
- 値が増加している場合、物理的な障害やネットワーク負荷の可能性を示します

### 📍 経路診断（MTR）

- **「診断開始」** を押すと、目的地のデバイスまでの経路上の全ルーターを自動探索します
- 各ルーターでの **遅延とパケットロス率** が表示されます
- どこで遅延・障害が発生しているかを特定するのに有効です

### 📈 履歴トレンド

- 過去24時間の **遅延（ms）と通信量（Mbps）** の推移をグラフで確認できます
- 異常な遅延スパイクの発生時刻などを遡って確認できます

---

## 6. 帯域計測（Iperf3 計測）

実際にデータを流して回線の最大速度をテストする機能です。

### 手順

1. 左サイドバーの **「Iperf3 計測」** タブをクリックします。
2. 計測対象の選択：
   - **登録済みデバイス**: ドロップダウンから **PCアイコンが設定されたデバイス** のみ選択できます
   - **カスタム入力**: 「カスタム入力に切替」を押して、iperf3サーバーのホスト名/IPを直接入力します
3. 必要に応じてオプション（ポート番号、プロトコルなど）を設定します。
   - ⚠️ オプションには **英数字・ハイフン・スペースのみ** 使用できます（セキュリティ上の制限）
4. **「計測開始」** を押すと、1秒ごとの通信速度がリアルタイムコンソールに流れます。
5. 計測が完了すると、結果が自動的に `Reports` フォルダ内に `.log` ファイルとして保存されます。

> **⚠️ 注意**: iperf3の計測はネットワーク帯域を大量に消費します。業務時間中の実施は避けることを推奨します。

---

## 7. 監視設定の変更

左サイドバーの **「通知設定」** セクションから以下を変更できます。

| 設定項目 | 説明 | デフォルト |
|----------|------|------------|
| **遅延アラート閾値 (ms)** | この値を超えた遅延でダッシュボードが警告色に変わります | 100 ms |
| **Pingデータサイズ (bytes)** | 監視パケットのサイズ（1〜65,500 bytes） | 1 byte |
| **監視間隔 (ms)** | Pingの実行間隔。短いほどリアルタイム性が上がりますが負荷も増えます | 1000 ms |

### 監視間隔について

| 間隔 | 特徴 |
|------|------|
| **1000 ms（推奨）** | 標準的な監視。複数台を同時監視可能 |
| **500 ms** | やや高頻度。複数台同時監視可能 |
| **100 ms（超高頻度）** | **1台のみ** 監視可能。「高頻度監視対象IP」で対象を指定します |

> ⚠️ **100ms以下の設定は、HighFreq対象として指定した1台のみ有効**です。複数台が登録されていても、超高頻度Pingは対象IP1台のみに実行されます。

- 設定は **自動で保存** され、次回起動時も引き継がれます。

---

## 8. ログ・レポートの確認

### Reports フォルダ

- `Reports/<起動日時>/` フォルダ内に以下が保存されます：
  - **`ping_<IP>.csv`**: Pingの応答履歴（日時、遅延ms、オンライン/オフライン）
  - **`iperf_<対象>_<日時>.log`**: Iperf3の計測ログ

- CSVは1ファイルが **5MB** を超えると自動的にリネームされ、最大 **3世代** 保持されます。

### デバッグログ（問題発生時）

- バックエンドで予期せぬエラーが発生した場合、`system/debug.log` にエラー内容が記録されます。
- 問題が発生した際はこのファイルを確認するか、管理者に提供してください。

---

## 9. 終了方法

1. ブラウザのタブを閉じます。
2. バックグラウンドで動いている **黒いPowerShell画面を閉じます**。

> 💡 PowerShell画面を閉じると監視が停止します。再度監視を開始するには `Start-Monitor.bat` を実行してください。

---

## 📂 フォルダ構成（参考）

```
NetworkDeviceMonitor/
├── Start-Monitor.bat      ← 起動はここをダブルクリック
├── 利用手順書.md          ← この文書
├── Reports/               ← 計測ログ・Ping履歴が保存される
│   └── <起動日時>/
│       ├── ping_<IP>.csv
│       └── iperf_*.log
└── system/                ← システム本体（通常は触らない）
    ├── Server.ps1
    ├── devices.json       ← 登録機器情報（自動保存）
    ├── config.json        ← 設定ファイル（自動保存）
    └── debug.log          ← エラーログ（問題発生時に確認）
```
